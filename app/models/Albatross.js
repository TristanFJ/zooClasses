export class Albatross {


  // Can I replace this constructor with a function (or something) in controller to make changing parameters easier? 
  constructor(type, name, weight, features, snack) {
    this.type = type
    this.name = name
    this.weight = weight
    this.features = features
    this.snack = snack

  }
}